-- ============================================================================
-- RECARGA DA DATA API
-- Execute quando uma view, tabela ou schema novo não aparecer no navegador.
-- Não altera nenhum dado.
-- ============================================================================

notify pgrst, 'reload schema';
